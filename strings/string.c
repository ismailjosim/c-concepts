#include <stdio.h>

int main()
{
    // *1: Declaring a String:
    char myString[30];

    // *2: Initializing a String:
    char greeting[] = "Hello, World!";

    // *2.1: print string
    // printf("%s", greeting);

    // *3: Assign a string value
    // strcpy(myString, "Well come to C programming!");

    // printf("%c", myString);

    // *** Others example
    char a[] = "ISMAIL JOSIM"; // * to end the null value we have to use \0
    printf("%s", a);

    return 0;
}

#include <stdio.h>

int main()
{
    int a = 10;
    int b = 20;
    if (a == b)
    {
        printf("Both Are Equal!");
    }
    else if (a > b)
    {
        printf("A is Higher then B");
    }

    else
    {
        printf("Both Are Not Equal!");
    }
    return 0;
}

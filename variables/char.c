#include <stdio.h>

int main()
{
    char letter = "A";
    // printf("%c", letter);

    char sentence[] = "Welcome to C Programming!"; // *** use Arrays for full sentence. Strings in C are null-terminated, which means they end with the null character '\0'. For example
    // printf("%s\n", sentence);

    // \n means new line
    // \t means tab
    // \\ means \ backslash

    return 0;
}
/*
take out the value of 10

we know, 8 bit = 1 byte



0 0 0 0 0 0 0 0 0
2n
*/
